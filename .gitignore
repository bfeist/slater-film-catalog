node_modules/
.local/

 Data directory - all catalog JSON, intermediate files
data/
input_indexes/
# Expert review exports (files_audit temporary output)
scripts/files_audit/exports/
# SQLite catalog database
database/

# Activity logs (.local/ already covers .local/logs and .local/dockerLogs)

# Static assets (large binary files — not tracked in git)
static_assets/

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/

# Virtual env
.venv/

# uv
uv.lock

# Environment — local overrides (see .env.example for template)
.env
.env.prod
.env.home

# Docker environment — local overrides (see .env.docker.example for template)
.env.docker

# Auth config (contains password hashes — never commit)
auth.config.json

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
